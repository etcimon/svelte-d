{
  "name": "svelte-d-kit-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "bun + ts + svelte-d consumer of a nested kit tree: layout, page, [id], error, dual host.",
  "scripts": {
    "test": "bun test"
  },
  "dependencies": {
    "svelte-d": "workspace:*"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}
