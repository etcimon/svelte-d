// Generated by svelte-d from {{SOURCE}}
//# svelte-d-ir orig={{SOURCE}}:1 kind=file parse={{PARSE}}
// libwasm.dom.compile!() walks @child / inject! / @connect.
// mixin NodeDef!"tag" → NamedNode → TagHtmlElementMap (HTMLButtonElement, …).
// List events: UnorderedList → HTMLArray + ArrayItemEvents + assignEventListeners.
// Do not edit — compile will overwrite.
module {{MODULE}};

import libwasm;
import libwasm.array;
{{IMPORTS}}
// Module nothrow matches engine goldens. wasm-eh cells must compile with
// --foptimize-nothrow=false or D try/catch landing pads are deleted.
// .await is allowed in wireAwait; do not wrap it in try/catch (landing pad
// is throwBoundary / a helper after rewind, never around the import).
// Author `import std.*` is wasm Phobos (spa-phobos cell), lifted above nothrow.
nothrow:
@safe:

{{BODY}}
